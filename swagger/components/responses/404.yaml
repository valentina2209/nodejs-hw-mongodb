description: Not Found
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          type: string
          example: fail
        code:
          type: integer
          example: 404
        message:
          type: string
          example: "Resource not found"
