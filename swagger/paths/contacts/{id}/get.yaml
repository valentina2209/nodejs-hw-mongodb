tags:
  - Contacts
summary: Get contact by ID
operationId: getContactById
description: Returns one contact with the given ID.
security:
  - bearerAuth: []
parameters:
  - name: contactId
    in: path
    required: true
    description: Contact ID
    schema:
      type: string
responses:
  '200':
    description: Contact found
    content:
      application/json:
        schema:
          $ref: ../../../components/schemas/сontact.yaml
  '404':
    description: Contact not found
    content:
      application/json:
        schema:
          $ref: '../../../components/responses/404.yaml'
